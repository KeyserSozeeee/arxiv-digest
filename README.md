# arxiv-digest

Daily arXiv digest from configured RSS categories.

## Run (Windows)
```powershell
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe -m arxiv_digest.main


---

## 2) Install dependencies (from repo root)
In the VS Code terminal:

```powershell
.\.venv\Scripts\python.exe -m pip install -r requirements.txt